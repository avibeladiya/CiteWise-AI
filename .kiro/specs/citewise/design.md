# Spec: Design

Editorial citation desk. Display serif (Newsreader), body grotesque (Outfit), mono for meta (IBM Plex Mono). Ink/paper neutrals, one steel accent.

Surfaces: landing (masthead + specimen + docket), workspace (three columns), method (pipeline).

Interaction: click `[n]` to audit. Pipeline stages visible while indexing.
