# Tech

- TanStack Start + React 19 + Tailwind v4 + Zustand
- pdf.js in the browser; pypdf in Python
- BM25 retrieval (no vector DB on the MVP)
- xAI Grok (`grok-4.5`) via server function / FastAPI
- Vercel (web) + Render (API)

Do not add auth or Postgres unless the product later needs accounts.
